# Verification record

Local validation: 2026-09-17, Windows 11, Flutter 3.44.4 stable, Dart 3.12.2.
SDK: D:/computer_work/Tools_work/flutter. The SDK is not on the machine PATH.

## Scope

All library code is Dart/Flutter. Runtime dependencies: Flutter SDK only.
The Android example uses a stock Java FlutterActivity. Gradle .kts files are
standard build configuration, not Kotlin UI or platform rendering.
No changes were made to the separate Multi account project.

## Checks

- flutter analyze: passed with no issues (lib, tests and example).
- Root flutter test: 23 tests passed. Example flutter test: 4 tests passed.
- Root tests cover style interpolation, nullable gradients, validation, theme
  interpolation, bounded clipping, blur fallback, group isolation, keyboard
  interaction, target blur during animation, reduced motion, captured dialog
  themes, navigation results, and all five route transitions.
- Four component golden baselines cover light, dark, high contrast and no blur.
- Example tests cover theme switching, 1280 px desktop, 390 px mobile, dialogs,
  and 320 px width with 1.6x text scaling.
- Six gallery/dialog baselines use actual Roboto and Material Icons fonts.
  Shadow rendering is enabled when producing visual baselines.
- flutter build web --release: passed; creates example/build/web.
- flutter pub publish --dry-run validates an approximately 89 KB archive
  before this verification document. It reports a warning for the missing
  real homepage/repository URL; therefore its process exit status is nonzero.
  A GitHub remote has not been configured. No upload/publish was performed.

## Visual review and fixes

The screenshots were generated by the real Flutter test renderer and opened
for inspection. Reviewed clipping, circular/straight/rounded rims, foreground
legibility, dark dialogs, responsive layout and fallback appearance.

Fixed a one-pixel preview overflow by increasing its vertical room, making its
height respond to text scaling, and allowing preview metadata to wrap. Replaced
a missing arrow glyph in the sample label. High contrast now uses a dark rim
on light surfaces and a light rim on dark surfaces. Filters with differing
parameters cannot share a backdrop key. Nested surfaces are isolated.

Baseline updates were followed by ordinary tests that compare against the saved
images. Pin both OS and Flutter version for pixel-identical golden comparisons.

## Platform limits

- Web release compilation succeeded. Browser UI automation failed to connect
  (nodeRepl.fetch request failed); no live browser-interaction claim is made.
  Interaction and visual checks instead use Flutter widget tests.
- Android debug build was attempted with the existing Android SDK at
  D:/computer_work/Tools_work/Android SDK. Gradle reached Android project
  configuration, then failed with CXX1101: NDK 28.2.13676358 is missing
  source.properties. No valid installed NDK source.properties was found.
  Repair/reinstall the NDK before retrying. The SDK was not deleted or
  reconfigured globally.
- No Android device/GPU frame-time measurement was performed. Low/adaptive
  modes are deterministic policies, not a hardware benchmark. No FPS claim.
- iOS/macOS/Linux were not compiled on this Windows host. The library itself
  contains no platform-specific imports; compatibility there is intended,
  not locally verified.

## Locations

- Public exports: lib/lightweight_liquid_glass.dart
- Implementation: lib/src/{style,theme,surface,backdrop,components,motion}.dart
- Interactive app: example/lib/main.dart
- Tests: test/ and example/test/
- Component screenshots: test/goldens/
- Gallery screenshots: example/test/goldens/
- Built web app: example/build/web/
- Usage / API / performance guide: README.md
- Release metadata: pubspec.yaml, CHANGELOG.md, LICENSE, .pubignore
- Full generated source-file inventory: doc/FILES.txt (relative to project root).

The package archive excludes development-only test fonts, goldens and build
outputs; the full repository keeps those assets and their upstream licenses.
